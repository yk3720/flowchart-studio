# ワークスペースレイアウト方針 — 3ペインリサイズ · ナビ配色

**状態:** PR-B 実装済（2026-06-25 · 3ペインリサイズ · ナビ左揃え）。**UX改善パック（§D〜§E · ナビ余白 · 比率改定 · ペイン幅リセット）は実装済**（2026-06-25）。**§F ナビタイトル・ペイン幅リセット移動は実装済**（2026-06-26 · §E と同時 · `c92f345`）。  
**関連:** [UI仕様.md](./UI仕様.md) · [表ペインUX方針.md](./表ペインUX方針.md) · [ADR-016](<../03_技術仕様/意思決定記録(ADR).md#adr-016-10-列順-v2--3-ペインリサイズ2026-06-25>) · [design-system.md](../design-system.md)

---

## 背景

| 現象                   | 原因                                                                  |
| ---------------------- | --------------------------------------------------------------------- |
| 表ペインを広くできない | 表:図 = `2fr:3fr` 固定（`FC_WORKSPACE_MAIN_GRID`） — **PR-B で解消**  |
| ナビ幅を変えられない   | `lg:w-[min(20%,240px)]` 固定 — **Panel 化済み · §D で中身幅も修正済** |
| 3領域が同系色で平坦    | ナビ・表・図すべて `flow-surface-muted` 系                            |

**方針決定（ユーザー合意）:** 段階導入は行わず、**最初からナビ｜表｜図の3ペインすべて**をドラッグリサイズ可能にする。一度の変更で総コストを最小化する。

---

## 決定事項

### A. 3ペインリサイズ

| 論点                        | 決定                                                                                                       | 理由                                                                                                |
| --------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 方式                        | **react-resizable-panels v4**（`Group` / `Panel` / `Separator`）                                           | 業界標準 · `useDefaultLayout({id})` で永続化 · Next.js SSR 対応。v3 以前と API 命名が異なる点に注意 |
| 対象                        | **ナビ ｜ 表 ｜ 図** の3本スプリッター                                                                     | 表↔図だけ後追いすると `FlowchartWorkspace` を二度触る                                               |
| 折りたたみナビ              | 既存 `navCollapsed` を **Panel `collapsible`** と統合検討                                                  | 二重の開閉ロジックを避ける                                                                          |
| モバイル                    | **lg 未満は現行どおり** タブ「表｜図」+ ナビはドロワー/折りたたみ                                          | リサイズはデスクトップ専用                                                                          |
| 永続化                      | `localStorage` · 外側: `flowchart-studio:workspace-outer-v2` · 内側: `flowchart-studio:workspace-inner-v2` | `useDefaultLayout({id})` フック経由 · SSR は `getWorkspaceLayoutStorage()` 必須                     |
| 初期比率（v1 · アーカイブ） | ナビ **18%** · 表 **40%** · 図 **60%**（表｜図は内側 Group で独立管理）                                    | PR-B 初期値                                                                                         |
| 初期比率（v2 · 現行）       | ナビ **18%** · 表 **52%** · 図 **48%**（内側 Group）                                                       | 縦長フロー向けに図ペインを約 1〜2 列分狭く · 表を広げる                                             |
| 下限（minSize）             | ナビ **160px** · 表 **400px** · 図 **280px**                                                               | 装置 select · 10列表 · RF 最低高を確保                                                              |
| 上限                        | ナビ **max 28%** または **320px**（実装時に計測）                                                          | 動作名 truncate とのバランス                                                                        |

### B. ナビ配色（2026-06-25 改定）

**経緯:** 初案はナビを深い青背景にする方針だった。Web 調査の結果、プライマリボタン（`--flow-accent` `#2563eb`）とフロー矢印（同色）と**色相が競合**し CTA の視覚階層が弱くなるため、**提案 A（ニュートラル面維持）** を採用。

| 論点             | 決定                                                                                                               | 理由                                                            |
| ---------------- | ------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| 背景             | **現行どおりニュートラル**（`flow-surface-muted` 系 · 表・図と同系統で可）                                         | 大面積に chromatic 青を載せない（Material / shadcn sidebar 型） |
| ゾーン分離       | **明度・ボーダー・タイポ**でナビを識別（全面の色面に頼らない）                                                     | 3 ペインが同系色でも問題なし（ユーザー合意）                    |
| 選択中モジュール | **左ボーダー `flow-accent`** + `flow-accent-selected-bg` / `flow-accent-selected-text`（現行パターン維持・強化可） | 青は**小面積アクセント**に限定                                  |
| プライマリ CTA   | **`--flow-accent` は再生成等の主操作のみ**（変更なし）                                                             | 明るい表ペイン上で最も彩度が高い要素にする                      |
| 表・図ペイン     | **明るい面維持**                                                                                                   | 編集・閲覧の可読性                                              |
| 却下             | ナビ全面の深い青（`#1e3a5f` 等）                                                                                   | CTA・`FLOW_EDGE_STROKE` と同色 family で階層が曖昧              |

**代替（将来検討のみ）:** ナビを**青ではない**暗いスレート（`#1e293b`）にし、選択だけ青アクセント — chromatic 青の大面積は依然として使わない。

**WCAG:** 選択・focus のコントラストは PR 前に計測。`text-flow-text-muted` を暗い面に載せる場合は 4.5:1 を確認。

### C. ナビ左揃え（既知バグ）

| 論点                 | 決定                                                                                                             |
| -------------------- | ---------------------------------------------------------------------------------------------------------------- |
| ユニット・動作ラベル | `fcNavUnitToggle` / `fcNavModuleBtn` に **`justify-start`** を追加（`fcTargetMin` の `justify-center` を上書き） |

本書のリサイズ実装と**同一 PR または直前 PR** で対応。**PR-B で実装済。**

### D. ナビ｜表ペイン間の余白（2026-06-25 実装済）

| 論点       | 決定                                                                            | 理由                                                                                       |
| ---------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 現象       | 第 1 ペイン（ナビ）と第 2 ペイン（表）の間に**空白**が見える                    | `ModuleNavPane` の `fcNavAside` が `lg:w-[min(20%,240px)]` 固定のまま Panel 幅に追従しない |
| 修正       | `fcNavAside` を **`w-full min-h-0 flex-1`** 等に変更し Panel 内を**全面に使う** | PanelGroup 導入後の名残 CSS を除去                                                         |
| ボーダー   | ナビ `border-r` と `Separator` の**二重線を解消**（どちらか一方に統一）         | 視覚的な「隙間」感の低減                                                                   |
| 折りたたみ | `fcNavAsideCollapsed` も同様に Panel 幅へ追従                                   | 48px 折りたたみ時も整合                                                                    |

### E. デフォルト比率改定 · ペイン幅リセット（2026-06-25 実装済）

| 論点                 | 決定                                                                                                          | 理由                                                                                                                            |
| -------------------- | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| デフォルト表｜図比率 | **52% : 48%**（v1 の 40:60 から変更）                                                                         | フローは縦長 · 図ペインはやや細くてよい · 表は 1〜2 列分広げる                                                                  |
| ナビ比率             | **18% 維持**                                                                                                  | 装置 select · ユニット名 truncate とのバランス                                                                                  |
| 永続化キー           | `workspace-outer-v2` · `workspace-inner-v2`（v1 からバージョンアップ）                                        | 新デフォルトを fresh install に反映 · 既存 v1 はリセットまで維持                                                                |
| ペイン幅リセット     | 表ツールバー **T4「ペイン幅をリセット」** · `useGroupRef` + `setLayout()` で outer + inner を v2 デフォルトへ | 列幅 T4 は廃止（[表ペインUX方針 §D](./表ペインUX方針.md#d-表ペイン縦優先レイアウト2026-06-25-実装済)）· **§F で N9 へ移動予定** |
| 表列幅リセット UI    | **なし**（T4 削除）                                                                                           | ヘッダードラッグ + `localStorage` で足りる                                                                                      |
| minSize              | v1 同値（ナビ 160px · 表 400px · 図 280px）                                                                   | 下限は変更なし                                                                                                                  |

---

## 目標レイアウト（デスクトップ · 現行コード · §D〜§F 実装済）

```
┌─ AppHeader（全幅）────────────────────────────────────────┐
│ 文脈 · [再生成] · その他 ▼                                │
├╞════════════╡╞══════════════════════╡╞══════════════╡┤
│ NavPane    ║ TablePane（やや広）    ║ Canvas（やや狭） ║
│ (全面使用) ║ 表が最上段             ║ 縦長フロー       ║
│ 装置 select║ [行を追加][ペイン幅…]  ║                  ║  ← N9/M12 等（§F 実装済）
│ ユニット ▼ ║ 10列表                 ║                  ║
│  動作…     ║ ▶警告/取込/ヘルプ      ║                  ║
└────────────┴────────────────────────┴──────────────────┘
     ↑              ↑                         ↑
  ResizeHandle  ResizeHandle            （右端）
  （余白なし）   52% : 48%
```

---

## 目標レイアウト（デスクトップ · PR-B · §D 適用前 · アーカイブ）

> **注:** §D〜§E 適用前。ナビ右に空白 · 表:図 = 40:60 · T4 は列幅リセット。

```
┌─ AppHeader（全幅）────────────────────────────────────────┐
│ 文脈 · [再生成] · その他 ▼                                │
├╞════════════╡╞══════════════════╡╞════════════════════╡┤
│ NavPane    ║ TablePane          ║ CanvasPane           ║
│ (ニュートラル) ║ (明るい面)         ║ (明るい面)           ║
│ 装置 select║ [行を追加][列幅…]  ║ フロー図             ║
│ ユニット ▼ ║ 10列表             ║                      ║
│  動作…     ║ ═══ 横スクロール ══║                      ║
└────────────┴────────────────────┴──────────────────────┘
     ↑              ↑                      ↑
  ResizeHandle  ResizeHandle          （右端）
```

---

## 実装フェーズ（推奨順）

| #   | 内容                                                             | 主なファイル                                    |
| --- | ---------------------------------------------------------------- | ----------------------------------------------- |
| 1   | `react-resizable-panels` 追加                                    | `package.json`                                  |
| 2   | `FlowchartWorkspace` を外側 `PanelGroup` 化（ナビ｜エディタ）    | `FlowchartWorkspace.tsx`                        |
| 3   | `FlowchartEditor` workspaceMode を内側 `PanelGroup` 化（表｜図） | `FlowchartEditor.tsx` · `flowchartUiClasses.ts` |
| 4   | `autoSaveId` + `defaultLayout`（SSR 用）                         | 同上                                            |
| 5   | ナビ `justify-start`（配色は現行維持）                           | `flowchartUiClasses.ts`                         |
| 6   | Playwright: スプリッタードラッグで表ペイン幅変化                 | `e2e/workspace-pane-resize.spec.ts`（新規）     |
| 7   | `ボタン一覧.md` · `UI仕様.md` 更新                               | docs                                            |

**PR-B 完了**（2026-06-25）。

### UX改善パック（§D〜§E · 完了）

| #   | 内容                                    | 主なファイル                                     |
| --- | --------------------------------------- | ------------------------------------------------ |
| 1   | ナビ `fcNavAside` 全面化 · ボーダー整理 | `flowchartUiClasses.ts` · `ModuleNavPane.tsx`    |
| 2   | 内側 defaultSize 52/48 · storage `-v2`  | `workspacePaneLayout.ts` · `FlowchartEditor.tsx` |
| 3   | T4 ペイン幅リセット · Group ref 配線    | `FlowchartWorkspace.tsx` · `FlowTableEditor.tsx` |
| 4   | 表ペイン縦優先（警告 details 等）       | [表ペインUX方針 §D](./表ペインUX方針.md)         |
| 5   | Playwright 追従                         | `e2e/*` · `npm run test:e2e:ux`                  |

**2026-06-25 完了**（E2E 9/9）

**削除・置換:** `FC_WORKSPACE_MAIN_GRID`（`lg:grid-cols-[2fr_3fr]`）は PanelGroup に置換。定数は deprecated コメントまたは削除。

---

## SSR · Next.js 注意

- `Group` に **`defaultLayout`** を渡し hydration shift を軽減（[react-resizable-panels SSR 例](https://react-resizable-panels.vercel.app/examples/persistent-layout/server-rendering)）
- パネル数・構成変更時は **`useDefaultLayout({id})` の `id` をバージョンアップ**（古い localStorage を無効化）
- `FlowchartWorkspace` は `"use client"` のため Group もクライアント内
- v4 では `PanelGroup`→`Group` · `PanelResizeHandle`→`Separator` · `autoSaveId`→`useDefaultLayout` と命名変更（詳細: `REACTFLOW_RULES.md §5.6-10`）

---

## テスト（最低限）

| 層         | 内容                                                                                                     |
| ---------- | -------------------------------------------------------------------------------------------------------- |
| Vitest     | なし（レイアウトのみ）                                                                                   |
| Playwright | スプリッター drag → 表ペイン `boundingBox` 変化 · reload 後比率維持 · モバイルではリサイズハンドル非表示 |
| 手動       | ナビ選択・focus のコントラスト（配色改定後も現行パターン）                                               |

---

## 調査参照（Web）

| トピック               | URL                                                                                                             |
| ---------------------- | --------------------------------------------------------------------------------------------------------------- |
| react-resizable-panels | https://www.npmjs.com/package/react-resizable-panels                                                            |
| 永続レイアウト         | https://react-resizable-panels.vercel.app/examples/persistent-layout                                            |
| WCAG コントラスト      | https://developer.mozilla.org/en-US/docs/Web/Accessibility/Guides/Understanding_WCAG/Perceivable/Color_contrast |

---

## 他資料との関係

| 資料                                                                                            | 関係                                                           |
| ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| [表ペインUX方針.md](./表ペインUX方針.md)                                                        | 列リサイズ（実装済）· **縦優先レイアウト · T4 差し替え（§D）** |
| [ADR-016](<../03_技術仕様/意思決定記録(ADR).md#adr-016-10-列順-v2--3-ペインリサイズ2026-06-25>) | 10列 v2 と同一マイルストーンで実装予定                         |
| [design-system.md](../design-system.md)                                                         | レイヤー C 更新（`--flow-nav-*` · PanelGroup）                 |

---

**2026-06-25 完了**（E2E 9/9）

---

### F. ナビタイトル · ペイン幅リセット移動（2026-06-26 · 実装済）

**背景:** 表ペインの縦スペース回収と、レイアウト系操作のナビ集約。[表ペインUX方針 §E](./表ペインUX方針.md#e-表ペイン-ux-改定パック2026-06-26--実装済) と同一 grill-me セッション。**実装:** `c92f345`。

**適用範囲:** デスクトップ 3 ペインのみ（§E と同じ）。

| 論点                | 決定                                                                                                      | 理由                         |
| ------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------- |
| ナビヘッダー文言    | **「フロー」→「Flowchart Studio」**（全文 · truncate 可）                                                 | 表ペインからタイトル行を除去 |
| ペイン幅リセット    | **N9** · N8（全ユニット折りたたみ）の横 · **アイコンのみ** · ホバー/`aria-label` で「ペイン幅をリセット」 | 表下 T4 から移動 · §E と整合 |
| 装置を削除          | **N3 廃止（ナビ）** → **M12（その他 · 危険）**                                                            | 低頻度 · select 下 1 行節約  |
| §E 表ペインタイトル | Flowchart Studio · 実用版バッジ · 「表」`h2` を **表ペインから削除**                                      | 縦スペース 1 行以上回収      |

**目標レイアウト（§F · ナビ部分 · 実装済）**

```
┌─ NavPane ─────────────────────┐
│ Flowchart Studio    [N8][N9]  │  ← N8=全ユニット展開/折りたたみ · N9=ペイン幅リセット
│ 装置 [select ▼]               │  ← N3（装置を削除）はここから除去
│ ユニット ▼ …                  │
└───────────────────────────────┘
```

**§E との関係:** 表ペイン側の変更は [表ペインUX方針 §E](./表ペインUX方針.md#e-表ペイン-ux-改定パック2026-06-26--実装済) が SSOT。本 §F はナビ・M 系の配置のみ。

**実装フェーズ（§F · 完了 · `c92f345`）**

| #   | 内容                          | 主なファイル                                                             |
| --- | ----------------------------- | ------------------------------------------------------------------------ |
| 1   | タイトル · N9 · N3 除去       | `ModuleNavPane.tsx` · `flowchartUiClasses.ts`                            |
| 2   | M12 装置削除 · fixed メニュー | `EditorMoreMenu.tsx` · `FlowchartWorkspace.tsx`                          |
| 3   | T4 配線を N9 へ               | `FlowchartWorkspace.tsx` · `FlowchartEditor.tsx`                         |
| 4   | E2E                           | `e2e/workspace-pane-resize.spec.ts` · `e2e/import-bundle.spec.ts`（M12） |

---

**最終更新:** 2026-06-26（§F **実装済** · §D〜§E **実装済**）
