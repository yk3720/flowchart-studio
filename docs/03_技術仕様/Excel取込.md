# Excel 取込 — 正規化パイプライン

**作成:** 2026-06-07  
**状態:** **Python v0.1 · Web 取込 v0.1 実装済み** · dev DB への **005 マイグレーション適用** · 手動 E2E 確認は未  
**関連:** [ADR-014](<./意思決定記録(ADR).md>) · [DB設計.md](./DB設計.md) · [作者ガイド.md](./作者ガイド.md) · [UI仕様.md](../02_機能設計/UI仕様.md)

---

## 1. 決定事項（2026-06-07）

| 項目             | 決定                                                                                    |
| ---------------- | --------------------------------------------------------------------------------------- |
| **正規化の正本** | **Python CLI**（Git · pytest · JSON 出力）                                              |
| **Power Query**  | **任意** — 作者が自分 PC で同じ取込用を作るショートカット。仕様・テストの SSOT ではない |
| **VBA**          | **使わない**                                                                            |
| **Web**          | **入力用 Excel を直接パースしない**（横並び→縦変換は Web 側却下 · ADR-014）             |
| **作者の入力**   | ユニットシート内のフロー表は **横並び可**（縦積みブロックは要求しない）                 |
| **保存先**       | Supabase DB-2（4 表 + `flow_documents`）— **RPC 1 トランザクション**                    |

---

## 2. 3 レイヤー

```
入力用.xlsx（作者）
    ↓  Python normalize（正本）
取込用 bundle（import.json 等）
    ↓  Web 一括取込
Supabase → 左ナビ + フロー表
```

| レイヤ     | 誰が触る                 | 内容                                       |
| ---------- | ------------------------ | ------------------------------------------ |
| **入力用** | 作者                     | 横並びフロー表 · 構成シート                |
| **取込用** | Python 生成 · Web が読む | 1 動作 = 1 つの 10 列表（正規化済み）      |
| **DB**     | Web（editor）            | 装置階層 upsert + `flow_documents.payload` |

---

## 3. 入力用 Excel（作者向け）

**テンプレ v0.1:** [`python/templates/入力用テンプレ_v0.1.xlsx`](c:/yk-application/flowchart-studio/python/templates/入力用テンプレ_v0.1.xlsx) · 再生成: `npm run excel:template`（リポジトリルート）

### 3.1 ファイル単位

- **1 ファイル = 1 社内番号**（`equipment_codes.internal_code` · 例: `Z00001`）
- フォルダ名と社内番号を揃える運用を推奨

**作者 Excel の置き場（パス SSOT · 2026-06-19）:**  
`c:/yk-application/flowchart-studio/python/testdata/devices/{社内番号}_{装置名}/`  
— 正本 `{社内番号}_{装置名}.xlsx`（フォルダ名と同名 · **Git 対象外**）· **共有は `import.json`** · **v0.3**（構成6列 + U0〜U9 + マスター表）· 1 動作試行 `_scratch/{動作名}.xlsx` · 一覧 [`testdata/devices/README.md`](c:/yk-application/flowchart-studio/python/testdata/devices/README.md)  
— 手書き作業中の実装置: **`A0001_塗布装置`**

### 3.2 シート構成

| シート         | 枚数           | 内容                                                   |
| -------------- | -------------- | ------------------------------------------------------ |
| **構成**       | **1**          | 装置階層一覧（左ナビの SSOT）                          |
| **ユニット名** | **ユニット数** | 当該ユニット内の **複数動作** のフロー表（**横並び**） |

**構成シート（4 列 · 1 行目ヘッダー）**

| 列       | 例           | DB                              |
| -------- | ------------ | ------------------------------- |
| 装置製番 | Z00001       | `equipment_codes.internal_code` |
| 装置名   | プレス機 B   | `devices.display_name`          |
| ユニット | 供給ユニット | `units.label`                   |
| 動作     | 取出         | `modules.label`                 |

- 行順 = Nav の並び（`sort_order`）
- **装置製番 = 社内番号**（用語をテンプレで固定）

### 3.3 ユニットシート — 横並びの最小ルール

作者の使い勝手のため **表は横に並べてよい**。正規化の安定性のため、**各動作ブロックを Excel の「テーブル」として登録**する（推奨 · 初版 SSOT）。

| ルール | 内容                                                                                             |
| ------ | ------------------------------------------------------------------------------------------------ |
| **R1** | シート名 = 構成シートの **ユニット列と完全一致**（Excel 31 文字制限に注意）                      |
| **R2** | 各動作 = **1 つの Excel テーブル**（挿入 → テーブル）                                            |
| **R3** | テーブル名 = **`{ユニット短名}_{動作名}`**（例: `供給_取出`）— **ブック全体で一意**（実装 v0.1） |
| **R4** | テーブル内 = **10 列**（[作者ガイド.md](./作者ガイド.md) · `table-10col-v1`）                    |
| **R5** | 結合セルは **テーブル内で使わない**                                                              |

> **却下:** 1 シート内の動作を縦積みのみで書かせる方式（入力しにくい）。

> **Power Query 利用時:** R2（Excel テーブル）が揃っていれば、作者 PC 上で取込用シートを更新できる。出力形式は Python と同一とする。

---

## 4. Python 正規化（正本）

### 4.1 置き場

`c:/yk-application/flowchart-studio/python/` · [README](c:/yk-application/flowchart-studio/python/README.md)

### 4.2 入出力

**入力:** 入力用 `.xlsx`

**出力:** `import.json`（または `import.xlsx` + `_取込` シート群 — **JSON を第一候補**）

```json
{
  "internal_code": "Z00001",
  "display_name": "プレス機 B",
  "units": [
    {
      "label": "供給ユニット",
      "sort_order": 0,
      "modules": [
        { "label": "取出", "sort_order": 0 }
      ]
    }
  ],
  "flows": [
    {
      "unit_label": "供給ユニット",
      "module_label": "取出",
      "title": "取出",
      "payload": {
        "schema": "table-10col-v1",
        "table": [ ["10", "端子", …] ]
      }
    }
  ]
}
```

### 4.3 処理手順

1. 「構成」シートをパース → `units` / `modules` リスト
2. 各ユニットシートを走査 → **Excel テーブル**（または名前付き範囲）ごとに 10 列データ抽出
3. テーブル名 ↔ `(unit_label, module_label)` を解決
4. **整合性検証**（§5）— 失敗時は非 0 終了 + 作者向けメッセージ
5. `import.json` 出力

### 4.4 テスト

- pytest + 入力用 fixture xlsx（1 装置 · 2 ユニット · 各 2 動作 · 横並び）
- 構成 ↔ フロー不一致 · 列数不足 · 未知テーブル名 をケース化

---

## 5. 整合性検証（Python · Web 共通）

| #   | 検証                                                             | 失敗時 |
| --- | ---------------------------------------------------------------- | ------ |
| V1  | 構成の `(ユニット, 動作)` ごとに `flows` が 1 件                 | エラー |
| V2  | `flows` の `(unit_label, module_label)` が構成に存在             | エラー |
| V3  | 各 flow の表が **10 列**（ヘッダー行の有無はパーサー規約で統一） | エラー |
| V4  | シート名と `unit_label` 不一致                                   | エラー |
| V5  | `internal_code` がファイル内で一意                               | エラー |

**警告（取込は続行可）**

- 構成にあるが `flows` が空 → 「フロー未登録」警告
- DB に存在するが構成に無い行 → **orphan 警告**（DELETE はしない · ADR-014）

---

## 6. Web 取込（v0.1 実装済み）

- **読むもの:** `import.json` のみ（Python 正規化出力）
- **入口:** その他メニュー → **「import.json を取込…」**（workspace · editor のみ）
- **保存:** Server Action `importEquipmentBundle` → RPC **`import_equipment_bundle`**（`005_import_equipment_bundle.sql`）
- **初版 upsert:** 名称マッチ · `sort_order` 更新 · 新規 INSERT — **prune / rename 非対応**

**dev 適用手順:** `005_import_equipment_bundle.sql` を Supabase Dashboard / CLI で 004 の後に適用。

---

## 7. 作者向けエラー（Python 出力 · 優先）

形式: **何が · どこで · どう直す**

1. 構成に動作があるが、ユニットシートに対応テーブルがない
2. シート名 ↔ 構成のユニット名不一致
3. 10 列不足 / ヘッダー不正
4. 接続先 ID が表内に存在しない（任意 · 警告でも可）
5. `internal_code` と Web 選択装置の不一致

---

## 8. 再取込ポリシー（作者向け · 1 行）

**構成から行を消しても Web / DB からは自動削除されない。** 誤登録は管理者（Runbook · 将来 admin UI）。

---

## 9. 未決（実装前）

| #   | 論点                                                                    |
| --- | ----------------------------------------------------------------------- | --------------------------------------------- |
| 1   | Excel テーブル名                                                        | **`{ユニット短名}_{動作名}`** — v0.1 確定     |
| 2   | `import.json` vs `import.xlsx`                                          | **JSON 第一候補** — v0.1 は JSON のみ         |
| 3   | Python CLI の配置                                                       | **`yk-application/flowchart-studio/python/`** |
| 4   | Power Query サンプルクエリを **テンプレに同梱するか**（任意 · SSOT 外） |

---

## 10. 参照

| ドキュメント                                                                              | 用途                     |
| ----------------------------------------------------------------------------------------- | ------------------------ |
| [grill-me_2026-05-31_DB-2装置階層.md](../01_要求定義/grill-me_2026-05-31_DB-2装置階層.md) | Q8–Q10 正規化 · VBA 却下 |
| [DB設計 §8](./DB設計.md)                                                                  | Excel パイプライン草案   |
| `flowchart-studio/lib/flowchart/table/tableColumns.ts`                                    | 10 列 SSOT               |

---

_実装順: テンプレ v0.1 → Python normalize → RPC → Web 取込 UI → **005 適用（dev）** → E2E 確認 → 作者向け Excel テンプレ_
